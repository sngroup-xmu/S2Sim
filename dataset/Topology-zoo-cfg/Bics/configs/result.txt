----------------------------bratislava----------------------------
(-1 +) 在此行后加入下列配置
peer 10.0.0.5 enable
peer 10.0.0.5 ebgp-max-hop 2
peer 10.0.0.1 enable
peer 10.0.0.1 ebgp-max-hop 2
(61 +) 在此行后加入下列配置
peer 10.0.0.5 as-number 80
peer 10.0.0.1 as-number 300
------------------------------------------------------------

----------------------------budapest----------------------------
(-1 +) 在此行后加入下列配置
peer 10.0.0.4 enable
peer 10.0.0.4 ebgp-max-hop 2
(67 +) 在此行后加入下列配置
peer 10.0.0.4 as-number 50
------------------------------------------------------------

----------------------------vienna----------------------------
(-1 +) 在此行后加入下列配置
peer 10.0.0.0 enable
peer 10.0.0.0 ebgp-max-hop 2
(63 +) 在此行后加入下列配置
peer 10.0.0.0 as-number 50
------------------------------------------------------------

